{
  "name": "@brainboom/db",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "push": "drizzle-kit push",
    "typecheck": "tsc --noEmit",
    "build": "echo 'DB package - no build needed'"
  },
  "dependencies": {
    "drizzle-orm": "^0.31.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.22.0",
    "typescript": "^5.4.5"
  }
}
